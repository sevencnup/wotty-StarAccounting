# 2026-04-15 - web-vue 接入真实登录与资产页

## 本次目标

在不切换正式入口的前提下，继续推进 `web-vue/` 从“仅框架壳”升级到“具备真实业务能力”的并行 Vue 主线。

## 本次交付

- 接入 Vue 主线 API 基础层：
  - 新增 `web-vue/src/lib/runtime.ts`
  - 新增 `web-vue/src/lib/api.ts`
  - 新增 `web-vue/src/types/index.ts`
- 升级 Vue 版登录鉴权：
  - `web-vue/src/stores/auth.ts` 从本地假登录切换为真实 `/api/auth/login`
  - 新增 `/api/auth/me` 会话恢复
  - token / 用户信息按 API 作用域独立存储
- 升级路由守卫：
  - `web-vue/src/router/index.ts` 对受保护路由执行会话恢复
  - `web-vue/src/views/LaunchView.vue` 在启动时恢复登录状态
- 迁移第一个真实业务页：
  - 新增 `web-vue/src/views/AssetsView.vue`
  - `/assets` 不再是占位页，已读取 `/api/assets`
  - 页面展示资产总估值、流动资产、资产结构和资产明细
- 补充界面细节：
  - `web-vue/src/views/LoginView.vue` 增加错误提示
  - `web-vue/src/views/RegisterView.vue` 优化登录跳转衔接
  - `web-vue/src/style.css` 增加资产页所需样式

## 验证

- `npm --prefix web-vue run build`
- VS Code Diagnostics 检查通过，当前无新增显式诊断错误

## 当前状态

`web-vue/` 已从“只复刻外壳”推进到：

- 可以真实登录
- 可以恢复会话
- 可以进入受保护路由
- 可以打开真实资产页并读取后端数据

## 后续建议

建议下一轮继续迁移：

1. 消费页
2. 储蓄页
3. 贷款页
4. Vue 主线与 Android 联调壳的调试配置页
