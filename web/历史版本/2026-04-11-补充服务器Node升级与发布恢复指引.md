# 2026-04-11 补充服务器 Node 升级与发布恢复指引

## 本次目标

把“服务器 Node 版本过低导致远程发布失败、前端站点返回 403”这类问题整理成仓库内可复用操作文档，方便后续直接照单执行。

## 本次完成

1. 新增 `docs/服务器Node升级与发布恢复指引.md`
   汇总 Debian/Ubuntu 与 CentOS/RHEL 的 Node 20 升级命令。
2. 新增 `docs/服务器Node升级与发布恢复指引.md`
   补充站点权限修复、PM2 重启、Nginx 校验与健康检查命令。
3. 更新版本记录文档
   同步 `CHANGELOG.md`、`docs/根目录一键打包发布脚本开发文档.md` 与 `docs/开发进度.md`。

## 结论

后续遇到同类问题时，先确认服务器 Node 版本，再确认静态目录权限与 `index.html` 是否存在，最后再排查 Nginx 配置。
