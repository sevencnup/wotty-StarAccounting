# 2026-04-15 - web-vue 首页深度视觉重构对齐图 2

## 本次目标

按照设计图 2 的高对比度、无边框、实心彩色数据卡片风格，对 `web-vue` 首页进行深度视觉重构，彻底抛弃旧版白底卡片拼凑感。

## 本次交付

- 更新顶部栏布局：
  - `web-vue/src/components/shared/TopHeader.vue`
- 更新首页结构与图表配置：
  - `web-vue/src/views/DashboardHomeView.vue`
- 更新卡片容器与颜色规范：
  - `web-vue/src/style.css`

## 本次重构细节

- **顶部工作台**：移除全宽搜索条布局，改为左侧 `Backend` 标题，右侧 `搜索框 + 通知图标 + 头像` 紧凑结构。
- **主图表区**：ECharts 折线图配置为平滑曲线（`smooth: 0.5`）、开启面积渐变、隐藏坐标轴和网格线；统一图表容器为柔和的大范围阴影白底卡片。
- **KPI 数据卡片**：将原有的顶部带颜色细边的白底卡片，重构为 **蓝、绿、白、蓝** 四张实心背景卡片；重新排列文字布局（大字号主数值），并在每张卡片内部使用 `vue-echarts` 渲染一条极简的迷你波浪线。
- **底部表格**：移除原有的右侧“预算风险”与“重点分类”堆叠卡片；让数据表格（Detal Catons）实现全宽铺满；表格行尾增加带透明背景的 `Success` 绿色状态药丸按钮。

## 验证

- `npm --prefix web-vue run build`
- VS Code Diagnostics 检查通过，当前无新增显式诊断错误。
