# 2026-04-11 修复发布脚本内嵌 Bash 变量与 JS 模板字符串冲突

## 本次目标

解决 `scripts/deploy.js` 中内嵌远程 Bash 脚本时，`$\{...\}` 被 JavaScript 模板字符串提前解析，导致本地执行阶段直接抛出语法错误的问题。

## 本次完成

1. 更新 `scripts/deploy.js`
   把远程 Bash 中保留给服务器端展开的 `\${REQUIRED_NODE}`、`\${CURRENT_NODE_PATH:-not found}`、`\${CURRENT_NODE_VERSION}` 做转义处理。
2. 完成本地校验
   `node --check scripts/deploy.js` 已通过。

## 结论

这次报错发生在本地 Node 解析 `deploy.js` 阶段，还没进入服务器执行。修复后重新拉取最新代码即可继续发版。
