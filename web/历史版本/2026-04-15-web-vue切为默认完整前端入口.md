# 2026-04-15 - web-vue 切为默认完整前端入口

## 本次目标

停止使用挂在旧 `web` 里的 Vue 预览 / 嵌套方案，改为让 `web-vue` 直接成为默认可运行、可见的完整前端入口。

## 本次交付

- 将根目录默认命令切到 `web-vue`：
  - `npm run dev`
  - `npm run build`
  - `npm run start`
- 保留旧 Next 参考入口：
  - `npm run dev:legacy`
  - `npm run build:legacy`
  - `npm run start:legacy`
- 新增 Vue 主线固定主端口脚本：
  - `web-vue/package.json` 新增 `dev:main`
  - `web-vue/package.json` 新增 `preview:main`
- 删除旧嵌套预览方案：
  - 删除 `scripts/sync-vue-preview.mjs`
  - 删除 `web/src/app/vue-preview/page.tsx`
  - 删除 `web/public/vue-preview-app/`

## 验证

- `npm --prefix web-vue run build`
- `npm --prefix web-vue run dev -- --port 3301`

## 当前效果

现在默认启动项目时，看到的是完整 `web-vue` 页面框架：

- Vue 路由
- Vue 侧边栏
- Vue 顶部栏
- Vue 登录与业务页面

旧 `web` 继续保留，但只作为参考实现，不再承载默认可见前端。

## 后续建议

后续继续基于这条完整 Vue 主线迁移：

1. 消费页
2. 储蓄页
3. 贷款页
4. 更多图表、弹层、筛选器和表格组件
