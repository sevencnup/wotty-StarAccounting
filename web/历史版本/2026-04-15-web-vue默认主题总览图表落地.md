# 2026-04-15 - web-vue 默认主题总览图表落地

## 本次目标

按旧 `web/src/features/dashboard/components/themes/DefaultDashboard.tsx` 的默认主题样式，在 `web-vue` 首页接入真正的图表模块，而不是继续停留在文字型总览卡片。

## 本次交付

- 更新首页：
  - `web-vue/src/views/DashboardHomeView.vue`
- 更新总览数据层：
  - `web-vue/src/features/dashboard/api.ts`
- 扩展总览类型：
  - `web-vue/src/types/index.ts`
- 更新默认主题图表样式：
  - `web-vue/src/style.css`

## 新增图表模块

- 收支趋势图
- 渠道结构环图
- 重点分类
- 资金健康面板
- 重点信号卡片

## 实现说明

- 继续复用现有 `/api/dashboard/summary`
- 趋势、分类、渠道结构等图表数据在 Vue 端基于真实流水做聚合
- 保持默认主题的信息层级和板块组织方式，而不是继续展示迁移说明占位内容

## 验证

- `npm --prefix web-vue run build`
- VS Code Diagnostics 检查通过，当前无新增显式诊断错误

## 后续建议

继续推进：

1. 资产 / 消费 / 储蓄 / 贷款页补齐默认主题图表区
2. 把当前 CSS 图表逐步替换为更完整的可复用图表容器
3. 收口剩余占位页
4. 继续对齐旧默认主题的细节表现
