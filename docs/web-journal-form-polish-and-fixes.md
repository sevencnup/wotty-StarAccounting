# Web 记账与新增表单体验精细化修复开发文档

## 1. 修复项清单
1. **移除自动聚焦呼出键盘**：移除金额输入框上的 `autoFocus` 属性，避免移动端面板一展开就弹出软键盘遮挡内容。
2. **升级全局记账/新增悬浮按钮 UI**：
   - 优化为高质感晶透微光渐变胶囊按钮，带立体高斯阴影与精致微动效，提升点击欲望。
3. **升级储蓄页面（添加储蓄）的弹窗 UI 质感**：
   - 统一储蓄添加界面的卡片风格、圆角与操作按钮。
4. **修复贷款页面（新增贷款）模块超出右边框问题**：
   - 贷款表单 `modern-form-grid-3` 在小屏下 3 列并排导致宽度撑破右边界，调整为响应式自适应网格，彻底防止右边框溢出。

---

## 2. 实施步骤

- [x] **步骤 1：修复 `JournalPanel.tsx`**
  - 移除 `amount-huge-input` 的 `autoFocus`。
  - 优化贷款表单的网格结构，避免小屏幕溢出。
- [x] **步骤 2：更新 `app/src/app/globals.css`**
  - 升级 `.global-journal-trigger` 悬浮按钮样式（高质感渐变、圆角高光、晶透立体感）。
  - 修复 `.modern-form-grid-3` 在移动端的自适应排版，杜绝右边溢出。
  - 升级储蓄计划表单在弹窗中的现代卡片样式。
- [x] **步骤 3：构建检查**
  - 运行 `next build` 确保无报错。
- [x] **步骤 4：本地 Git 提交与文档归档**
