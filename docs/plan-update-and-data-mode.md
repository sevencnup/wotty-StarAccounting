# 更新与本地/线上切换方案

版本：`0.0.49`

## 目标

1. 支持 App 内检查新版本。
2. 支持下载 APK，并拉起系统安装。
3. 支持在 App 内切换 `本地离线版` 与 `线上云端版`。
4. 允许用户输入域名绑定自己的线上部署地址。

## 当前状态

1. `web` 侧已存在 `LOCAL/CLOUD` 的概念，但 `CLOUD` 还没有真实远程仓库实现。
2. 现有数据主要落在 IndexedDB，本地可用，线上不可切。
3. `api-server` 已有 Ktor + MySQL 的后端骨架和一组路由占位，但还没形成完整可用的同步 API。

## 方案一：APK 更新

### 实现方式

1. 后端提供版本接口：`versionCode`、`versionName`、`apkUrl`、`changelog`、`forceUpdate`。
2. App 启动或设置页请求版本接口。
3. 版本过低时弹窗提示更新。
4. 下载 APK 到本地。
5. 调起 Android 安装器完成安装。

### 难点

1. 不能静默安装，必须用户确认。
2. Android 8+ 需要处理“允许安装未知应用”权限。
3. 下载包需要校验，避免包被篡改。
4. 若上架 Google Play，更新策略要改成官方 In-App Updates。

## 方案二：本地/线上切换

### 目标架构

1. `LOCAL`：继续使用 IndexedDB，保证离线可用。
2. `CLOUD`：新增远程仓库，走 `api-server`。
3. `DataModeManager` 根据配置返回不同仓库。
4. 用户在设置页输入域名后，保存为当前云端地址。

### 实现步骤

1. 定义统一的远程 API 协议。
2. 新增 `RemoteRepository`，实现与 `DataRepository` 对齐。
3. 扩展 `DataModeManager`，支持真正切换本地/线上。
4. 增加域名配置、连接测试、绑定保存。
5. 增加同步机制，至少支持首次全量拉取。
6. 再补增量同步和冲突处理。

### 难点

1. 不是单纯加一个按钮，核心难点是同步。
2. 本地和线上都能改数据时，需要冲突解决策略。
3. 域名切换后要处理登录态、缓存、数据归属。
4. 现有 `api-server` 仍需补全实体、表结构、接口实现。
5. 如果只做“切换地址不做同步”，体验会像两个独立系统。

## 落地架构

```text
App
├─ 本地模式
│  └─ IndexedDB / 本地仓库
├─ 线上模式
│  ├─ 用户输入域名
│  ├─ RemoteRepository
│  └─ api-server
└─ 同步层
   ├─ 首次全量拉取
   ├─ 增量上传/下载
   └─ 冲突处理
```

## 建议落地顺序

1. 先做 APK 更新接口和安装流程。
2. 再做云端模式的只读连通。
3. 然后做写入能力。
4. 最后补同步、冲突和域名切换体验。

## 版本记录

### `0.0.49`

1. 撤销 `0.0.48` 的首页单层化精简调整，恢复此前的液态玻璃模块样式。
2. 恢复首页本月收支趋势折线图和收支类型占比圆图。

### `0.0.48`

1. 首页删除本月收支趋势折线图和收支类型占比圆图，恢复聚焦核心财务摘要的紧凑结构。
2. 首页顶部收支汇总卡改为单层淡蓝液态玻璃，移除外层卡片与内层面板叠加形成的双层边框感。
3. 首页贷款、预算、大额支出和待处理列表取消内嵌圆角底板与描边，仅通过排版、留白和状态色组织信息。

### `0.0.47`

1. Web 首页重设计为纯色液态玻璃视觉，顶部收支汇总卡和首页模块不再使用可见渐变色。
2. 首页新增本月收支趋势与收支类型占比模块，使首页总览信息更完整。
3. 统一首页卡片圆角、半透明层、阴影、字体层级与行块样式，预算、待处理、贷款等模块改为更紧凑的玻璃面板。

### `0.0.46`

1. Web 储蓄页重设计为新的储蓄仪表盘结构，顶部总览卡展示累计储蓄、目标缺口和总进度环。
2. 新增本月计划、目标缺口、完成率三个紧凑指标模块，并重排目标组与月度节奏模块。
3. 储蓄页统一为大圆角液态玻璃视觉，去掉旧卡片边框和最近计划分割线，减少模块间距与横向溢出风险。

### `0.0.45`

1. 将 Web 报表月份固定为 `2026-01`，并同步整理 `default` 演示账本的 1 月流水与储蓄计划数据。
2. 修复储蓄页最近计划列表在窄屏撑宽页面的问题，页面整体禁止横向溢出。
3. 上调贷款页和资产页的标题、正文、金额与辅助说明字号，使多个财务页面的文字层级更一致。

### `0.0.44`

1. App 端与 Web 端新增演示数据自动种子：首次使用且仓库无任何业务数据时，自动种入一套当月演示数据（发薪、餐饮、购物、交通、娱乐流水，工资卡/微信/支付宝资产，全局与餐饮预算，房贷车贷，旅行基金与应急储备储蓄目标及储蓄计划）。
2. App 端 `DataRepository` 接口新增 `seedDemoData()`，`LocalRepository` 直接写入本地 JSON，`CloudRepository` 转发后端 `/api/sync/demo`；`MainViewModel.refreshData()` 检测到交易/资产/贷款/储蓄目标全空时自动种子并重新加载。
3. Web 端 `LocalRepository.seed()` 在无业务数据时种入同套演示数据；云端模式 `FallbackRepository.read()` 在云端返回空数组时回落到本地，保证所有页面（首页、消费、储蓄、贷款、账户）首次打开即有数据，不再出现全空页面。
4. Web 端统一报表月份由固定 `2026-01` 改为跟随当前月份动态计算，演示数据按月写入，统计口径与实际数据始终一致。

### `0.0.43`

1. Web 端统一报表月份改为 `2026-01`，首页月份标题与汇总口径同步切换到 2026 年 1 月。
2. 默认云端演示账本改用可展示的 1 月演示流水与储蓄计划，避免页面读取 `default` 账本时出现空白。
3. 修正消费平台图表的报表月份默认值实现，兼容当前 TypeScript 检查与本地测试方式。

### `0.0.42`

1. Web 端新增统一报表月份配置，当前统计月份固定为 `2026-07`。
2. 首页、消费页、消费图表、储蓄页和贷款页的本月统计统一读取 7 月数据，避免当前 8 月无流水导致页面显示为空。
3. 首页月份选择器同步显示 `2026年7月`，与实际统计口径保持一致。

### `0.0.41`

1. 首页顶部收支汇总卡从白色玻璃改为单色蓝色玻璃，保持无渐变但保留重点模块识别度。
2. 在 tab 布局层新增全局液态玻璃容器，将玻璃卡片效果扩展到消费、储蓄、贷款、资产和账户页面。
3. 全页面模块间距继续收紧，并为消费汇总、贷款仪表盘、资产净值和账户钱包等特殊模块补充一致的玻璃覆盖样式。

### `0.0.40`

1. 首页液态玻璃背景改为纯白，不再使用蓝绿环境渐变。
2. 首页顶部收支汇总卡移除渐变色，改为单层白色玻璃卡，并同步调整文字为深色体系。
3. 首页模块间距与卡片内边距整体压缩，减少上下左右留白。

### `0.0.39`

1. 首页顶部收支汇总模块确认存在外层玻璃卡与内层汇总卡两层可见材质。
2. 首页液态玻璃模式下将 `overview-card` 改为透明包装层，只保留 `overview-hero` 一层可见玻璃。
3. 顶部大模块继续保留蓝绿玻璃效果，但不再出现外层模块轮廓。

### `0.0.38`

1. 首页液态玻璃模块移除半透明硬边框和顶部汇总卡内描边，避免模块边缘看起来像双层外壳。
2. 首页玻璃卡片改为更柔和的内高光与投影，保留磨砂透明质感但弱化边缘线条。
3. 首页列表分割线在液态玻璃模式下降低透明度，让模块内部层次更轻。

### `0.0.37`

1. 首页模块试验 iOS 风格液态玻璃视觉，增加专属浅色玻璃背景、半透明卡片、背景模糊和内高光。
2. 首页核心汇总卡改为蓝绿半透明玻璃材质，保留原有收支、结余与发薪日交互。
3. 首页进度、汇总、贷款、预算和待处理模块统一加大圆角与玻璃态控件，不改变数据逻辑。

### `0.0.36`

1. 储蓄主页从六个模块收敛为核心概览、月度执行节奏和最近计划三块。
2. 移除年度曲线、目标分层和本月热力日历，减少页面纵向长度和重复信息。
3. 顶部概览改为轻量白卡，保留已存金额、目标进度、本月计划、待执行和已完成笔数。

### `0.0.35`

1. 储蓄主页重新改造为目标驾驶舱、年度曲线、月度执行节奏、目标分层、本月计划热力和最近计划六个模块。
2. 储蓄主页指标改为区分计划总额、已完成金额、待执行金额、目标进度和月均计划，减少单一汇总卡的信息挤压。
3. 新模块补充移动端紧凑布局和金额省略策略，避免 430px 宽度下文字与金额互相重叠。

### `0.0.27`

1. 记账/添加储蓄页顶部遮罩调整为更轻的半透明玻璃效果，降低灰色遮挡和模糊强度。

### `0.0.26`

1. 记账/添加储蓄页顶部遮罩从半透明白色改为半透明深灰玻璃层，避免底页为白色时仍显示成纯白。

### `0.0.25`

1. 添加储蓄页移除辅助说明文案，保留更精简的字段与操作结构。
2. 储蓄计划表格列宽继续压缩，减少横向滚动距离并提升小屏展示密度。
3. 记账/添加储蓄页恢复底层标签页缓存显示，上半部分重新变为半透明覆盖效果，同时避免重复渲染双层面板。

### `0.0.24`

1. 添加储蓄页新增“储蓄名称”输入，可填写存钱要买什么并随目标一起保存。
2. 储蓄目标新增存储类型切换：死期、现金、他人帮存；旧的 `FIXED_TERM` 数据会自动映射到“死期”。
3. 储蓄主页汇总卡新增当前储蓄名称和存储类型展示，前后页信息保持一致。

### `0.0.23`

1. 添加储蓄计划初始化增加 `try/catch/finally`，任何读取或写入异常都会释放加载状态。
2. 首次没有储蓄目标时先渲染默认本地表格，再异步尝试保存默认目标，避免卡在“储蓄计划加载中”。
3. 保存计划增加失败兜底提示，并确保保存异常后按钮状态会恢复。

### `0.0.22`

1. 云端模式的写入操作增加超时兜底，云端不可达时自动写入本地仓库。
2. 修复添加储蓄页初始化默认储蓄目标或保存计划时可能一直显示“储蓄计划加载中”的问题。

### `0.0.21`

1. 添加储蓄页表格列宽整体压缩，减少小屏横向滚动距离。
2. 储蓄计划第一行输入薪资、支出列或预计存后，会自动同步填充当前模式下后续月份。
3. 添加储蓄页关闭改为直接回到储蓄页，避免历史栈中存在记账页时出现关闭后一层又一层的页面感。

### `0.0.20`

1. `web` 云端模式增加全局读取兜底：云端接口失败或超时后自动读取本地仓库。
2. 本地仓库读取也失败时返回空数据，避免首页、消费、资产、贷款、账户等页面骨架屏一直加载。
3. 储蓄页移除页面内重复兜底逻辑，统一依赖 `DataModeManager` 的数据仓库降级策略。

### `0.0.19`

1. 修复底部导航跳转异常时过渡骨架屏一直覆盖页面的问题。
2. Tab 过渡骨架增加最长 1.2 秒保护，路由未及时完成时自动释放，避免页面看起来永远加载中。

### `0.0.18`

1. 修复储蓄页云端请求长时间挂起时骨架屏一直加载的问题。
2. 储蓄页加载云端数据增加超时保护，超时后自动回退本地仓库；本地读取也超时则展示空看板。

### `0.0.17`

1. 修复储蓄页云端接口不可达时 `Failed to fetch` 直接导致页面崩溃的问题。
2. 储蓄页加载云端数据失败后会回退读取本地仓库；本地也不可用时显示空看板，页面不再中断。

### `0.0.16`

1. 储蓄主页改回与消费页一致的卡片信息流布局，移除主页上的大表格编辑器。
2. 新增储蓄顶部汇总卡、储蓄计划进度、每日存入日历和最近储蓄模块。
3. “添加储蓄”底部入口继续进入 `/journal/savings`，添加页保留储蓄计划表编辑能力。

### `0.0.15`

1. 储蓄主页恢复为独立的“储蓄计划”总览表格页面，保留顶部汇总、单月存/隔月存切换、动态支出列和月度计划表。
2. “添加储蓄”入口继续使用独立 `/journal/savings` 页面，避免储蓄主页被添加页组件样式影响。

### `0.0.14`

1. 修复储蓄页底部“添加储蓄”入口仍可能打开普通记账页面的问题。
2. 储蓄入口现在写入储蓄上下文并跳转 `/journal/savings`，普通 `/journal` 也会在客户端读取兜底标记。
3. `/journal` 与 `/journal/savings` 都纳入记账页路由判断，避免储蓄添加页继续显示底部全局按钮。

### `0.0.13`

1. 储蓄页底部按钮改为打开独立的“添加储蓄”页面，不再沿用记账内容。
2. 新增 `/journal/savings` 路由，复用原有弹层式页面结构，但标题与主体内容切换为储蓄计划表。
3. 储蓄计划逻辑抽离为可复用组件，储蓄主页与“添加储蓄”页面共用同一套表单与保存逻辑。

### `0.0.12`

1. 储蓄页重构为可配置储蓄计划表，支持“单月存”12 行与“隔月存”6 行两种模式切换。
2. 储蓄计划表固定“薪资、预计存、剩余”列，支持动态新增和删除支出列，并按 `薪资 - 支出 - 预计存 = 剩余` 实时计算。
3. 储蓄页全局按钮改为“添加储蓄”，点击后保存当前整张计划表；同时补充储蓄计算测试。
4. 最近交易列表调整列宽与省略策略，避免第二列交易来源与右侧时间重叠。

### `0.0.1`

1. 初版方案文档。
2. 覆盖 APK 更新、离线/线上切换、实现步骤与风险点。

### `0.0.2`

1. 补充本地 + 远程落地架构图。
2. 补充开发顺序说明。

### `0.0.3`

1. Android 端增加云端地址保存、连接测试和本地/云端模式切换。
2. 后端增加健康检查和版本信息接口。

### `0.0.6`

1. 首页“本周待处理”改为金额与状态同排展示，右侧信息更紧凑。
2. 首页“异常支出提醒”改为“大额支出”，并去掉该区域的背景色。
3. 记账页改为更高的底部面板，并保留底层页面内容，修复打开时遮罩不透底和白屏闪烁问题。
4. 修复消费页服饰分类图标路径错误。

### `0.0.7`

1. `api-server` 新增 MySQL 持久化同步表 `sync_records`，启动时自动建表。
2. 新增 `/api/sync` 读写接口，支持 `web` 端以统一仓储模型访问云端数据。
3. `web` 新增 `RemoteRepository`，`DataModeManager` 现已真正支持 `CLOUD` 模式。
4. 新增 `/api/sync/demo` 演示数据写入接口，可一键写入账本、交易、资产、预算、贷款、储蓄目标等样例数据。

### `0.0.8`

1. `api-server` 的 `/api/sync` 改为直接读写本地 MySQL `star_accounting` 真实业务表，不再依赖临时同步表。
2. 保留 `web` 侧统一的同步接口协议，前端无需改协议即可使用数据库中的真实账本、交易、资产、预算、贷款与储蓄数据。
3. 修复 `/api/sync/demo` 演示写入接口的序列化问题，支持将演示账本数据按真实表结构写入数据库。

### `0.0.9`

1. `web` 启动时默认强制切换到 `CLOUD` 模式，避免继续读取浏览器本地假数据。
2. `LocalRepository` 不再自动灌入演示交易、资产、预算、贷款与储蓄假数据，只保留最小本地账本骨架。
3. 首页汇总逻辑移除折线图、占比、预算提醒、任务和洞察的演示回退数据，页面数据改为完全以数据库返回为准。
4. 为 `star_accounting` 增加可复用的 Web 演示数据脚本，直接写入真实业务表。

### `0.0.10`

1. 修复消费页每日收支柱状图遇到“京东”等非预设支出平台时，归入“其他”却未初始化数据序列导致的页面崩溃。
2. 平台图例与柱状图统一使用相同的归类规则，并增加未知平台回归测试。

### `0.0.11`

1. 消费流向桑基图为右侧消费类型标签预留完整显示空间，避免长分类名称被图表边界裁切。
2. 消费页最近流水按账务方向显示金额颜色：支出红色、收入绿色、转账灰色。
3. 核对首页资产、储蓄、贷款与消费数据链路，确认展示汇总均读取同一 `default` 云端账本；记录账户余额、贷款余额与记账流水尚未自动联动的现状。

### `0.0.5`

1. `web/package.json` 新增 `apk` 一键打包命令。
2. 命令会串行执行 Web 静态构建、Capacitor Android 同步与 Debug APK 打包。
3. 升级 `web/android` 的 AGP 与 Gradle Wrapper，修复 `Unsupported class file major version 65` 导致的打包失败。

### `0.0.4`

1. 后端启动改为数据库初始化失败时仍保留健康检查与版本接口可用。
2. 增加 `api-server/settings.gradle.kts`，支持后端单独编译验证。

### `0.0.28`

1. 全局记账与添加储蓄面板改为在当前页面原地打开，底层页面保持挂载并可透过遮罩显示。
2. 移除缓存路由节点导致的记账页重复渲染，修复弹层出现两层页面和顶部纯白的问题。
3. 记账遮罩改为轻量半透明效果，并移除模糊与顶部阴影。

### `0.0.29`

1. 记账与添加储蓄面板的遮罩恢复磨砂半透明效果，使用半透明浅色蒙层与背景模糊，同时保留底层页面轮廓。

### `0.0.30`

1. 压缩储蓄计划表的表头、数据行和输入框高度，提高 12 个月计划的纵向信息密度。
2. 增强记账与添加储蓄面板顶部边界线，在磨砂背景上保持清晰分隔且不使用阴影。
3. 隐藏 Next.js 16 开发环境右下角错误提示入口，避免遮挡储蓄计划表内容。

### `0.0.31`

1. 贷款页重构为债务仪表盘、最近还款、月供压力、还款日程和贷款组合，月供压力读取本月真实收入计算。
2. 资产页重构为净资产负债表、资产构成、流动性/负债率/集中度体检和双栏资产负债清单，接通储蓄及贷款汇总。
3. 账户页重构为钱包中心，支持账户类型筛选、账户活跃信息、资金集中度和差异化账户卡片。
4. 三个页面使用统一蓝灰主题、细边框和零阴影，但采用不同信息架构；新增专门设计与数据口径文档。

### `0.0.32`

1. 移除资产页净资产模块底部的资产负债彩色条，以及资产/负债清单顶部的绿红装饰边框。
2. 账户页新增常规设置，支持默认账户、账户排序、余额显示和流水自动匹配。
3. 账户设置即时影响页面展示并保存至浏览器本地配置，刷新后继续生效。

### `0.0.33`

1. 账户页改为分组式应用设置中心，包含模式切换、账单导入、主题、语言、字体大小、帮助与反馈和关于。
2. 七项设置使用统一底部面板展示详细内容，账户页隐藏全局记账浮动按钮。
3. 修复数据模式管理器初始化时强制切回云端的问题，本地与云端模式现在可真正切换并持久保存。
4. 新增微信、支付宝 CSV 账单解析和批量导入，兼容前置说明行、引号字段及 UTF-8/GB18030 编码。

### `0.0.34`

1. 账单导入拆分为微信与支付宝两个独立入口，分别按各自官方 CSV 字段解析，不再混用格式。
2. 上传文件与所选平台不一致时拒绝导入并显示明确提示，避免错误流水写入。
3. 云端模式新增连接测试和确定操作，使用后端 `/api/health` 验证服务地址。
4. 当前云端地址只有测试成功后才能确认切换；修改地址后必须重新测试。
