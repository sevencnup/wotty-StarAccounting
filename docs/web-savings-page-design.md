# Web 储蓄页液态玻璃设计

## 目标

在不改变储蓄数据、计算和交互的前提下，让储蓄页与首页保持同一套液态玻璃视觉语言，适配 420px 移动端。

## 本次调整

- 顶部“储蓄总览”保留浅薄荷色语义，但改为白色/浅绿色半透明渐变、内高光、低强度阴影和 `backdrop-filter` 玻璃层；左侧装饰色条不再显示。
- 三项关键指标改为轻量内层玻璃卡，使用半透明背景、细白边和内高光，避免三个模块看起来像不透明色块。
- “目标组”和“月度节奏”外层卡统一为 22px 圆角、白色渐变玻璃、内高光和柔和阴影；目标卡、月度卡片作为更浅的内层玻璃。
- “最近计划”卡与其他外层卡统一玻璃背景；最近计划行保留金额右侧对齐，并改为轻量玻璃内层。
- 目标进度条和月度节奏条统一为 8px 轨道，加入首页同款顶部微光罩层；绿色仍表示已完成/储蓄进度，蓝色仍表示计划范围。
- 环形总进度继续保留绿色，不改变百分比计算。

## 不改

- `web/src/app/(tabs)/savings/page.tsx` 的数据和 JSX。
- 储蓄计划编辑、浮动记账面板、底部导航。
- Android 与其他页面业务逻辑。

## 涉及文件

- `web/src/app/globals.css`
- `web/package.json`（版本 0.0.67 → 0.0.68）
- `docs/web-savings-page-design.md`
- `docs/web-home-page-design.md` 计划文件已同步本次背景与验证方案。

## 横向手势修复

- 页面式“添加储蓄”弹层只在顶部非屏幕边缘区域识别横向关闭，避免与浏览器边缘返回手势竞争。
- 左边缘起始手势不会触发弹层关闭；非边缘区域保留原有双向横滑关闭行为，垂直滚动不会误关闭。
- 页面遮罩和弹层增加横向 `overscroll-behavior` 约束，同时保留储蓄计划表格自身的横向滚动。

## 验证

- `web` 目录 `pnpm typecheck` 通过。
- Playwright 使用已安装的 Edge，以 420×900 访问 `/savings`：页面正常渲染，弹层可以打开。
- 移动触摸回放确认：左边缘向右滑时弹层保持打开，`body` 与文档横向滚动位置保持为 0；弹层非边缘区域向左/向右滑仍可关闭。
- 页面无横向溢出，储蓄计划表格仍保留自身横向滚动区域。
- 已知的本地 8080 `api-sync` 连接失败不属于本次页面改动。
