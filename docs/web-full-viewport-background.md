# 页面全视口背景修复

## 目标

修复标签页内容高度不足时，两侧或底部露出白色的问题，让所有页面的浅灰背景覆盖完整视口。

## 修改内容

- 标签页最外层布局改为使用全局浅灰背景变量。
- `html` 和 `body` 增加最小高度，保证短页面也能铺满视口。
- 保留底部导航栏自身的白色表面。

## 验收结果

- 首页、消费、储蓄、资产、贷款、设置页面的背景覆盖完整视口。
- 宽屏下内容区两侧不再露出白色。
- 页面内容结束后的底部留白保持浅灰。
- 移动端无横向溢出。

## 版本记录

本次为页面背景覆盖范围的视觉修复，不单独提升应用版本号。
