# 底部导航栏图标替换开发文档

## 目标

将 Web 底部导航栏切换为 `web/public/nav-icons` 中的新版图标，确保导航项不再引用已删除的旧文件。

## 实施步骤

- [x] 更新底部导航组件中的 6 个图标资源路径。
- [x] 检查新版 PNG 均可从 `/nav-icons/` 公共路径访问。
- [x] 运行类型检查和生产构建。
- [x] 完成本地 Git 提交。

## 验收标准

1. 首页、消费、储蓄、贷款、资产、账户底部导航分别显示对应的新图标。
2. 当前页面高亮状态和导航跳转行为保持不变。
3. 不再请求已删除的 `shouye.png`、`xiaofei.png`、`chuxv.png`、`daikuan.png`、`zichan.png`、`zhanghu.png`。

## 版本记录

本次为图标资源路径的小型视觉调整，不新增版本号。
